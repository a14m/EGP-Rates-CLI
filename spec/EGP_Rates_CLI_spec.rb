require "spec_helper"

describe EGPRatesCLI do
  it "has a version number" do
    expect(EGPRatesCLI::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
